# UE0.1 — Bootstrap C++

Status: implementado; validação consolidada pendente.

O bloco entrega projeto C++ em Unreal Engine 5.8.2, plugins-base, Gameplay Tags
iniciais, mapa vazio, teste Automation, Data Validation por commandlet e script
de validação consolidada. Python é habilitado exclusivamente para gerar o mapa
no Editor; não integra runtime.

O próximo bloco é UE0.2: manifest e fixtures. Nenhum modelo, animação ou sistema
de combate do KashmirAct entra no projeto antes de UE0.2.
